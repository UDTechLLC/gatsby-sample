import Typography from "typography"

const typography = new Typography({
  baseFontSize: "1.2em",
  baseLineHeight: "1.7em",
  headerFontFamily: ["Roboto", "font-sans"],
  bodyFontFamily: ["Roboto", "font-sans"],
})

export default typography